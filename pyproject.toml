[project]
name = "agentops-eval-platform"
version = "0.1.0"
description = "Local-first evaluation, monitoring, and debugging platform for three existing agents."
requires-python = ">=3.10"

[tool.unittest]
start-dir = "tests"
